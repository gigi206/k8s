---
# =============================================================================
# Calico NetworkPolicy - KubeVirt Manager Ingress from Envoy Gateway
# Calico equivalent of: cilium-ingress-policy-envoy-gateway.yaml
# =============================================================================
# Allows access to KubeVirt Manager web UI from Envoy Gateway
# Internal communication is handled by calico-ingress-policy.yaml
# =============================================================================
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: kubevirt-ingress-envoy-gateway
  namespace: kubevirt
spec:
  selector: all()
  types:
    - Ingress
  ingress:
    # Allow from Envoy Gateway (envoy-gateway-system namespace)
    - action: Allow
      protocol: TCP
      source:
        namespaceSelector: 'kubernetes.io/metadata.name == "envoy-gateway-system"'
      destination:
        ports:
          - 8080    # KubeVirt Manager HTTP
