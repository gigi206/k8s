---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - httproute.yaml
  - referencegrant.yaml
  - dashboard-service-https.yaml
  - serverstransport.yaml
  # Note: oauth2-proxy-referencegrant.yaml removed - Ceph uses SAML2 SSO, not OAuth2 Proxy
  # Note: Custom Service with appProtocol: https enables APISIX to detect HTTPS backend automatically
