---
# =============================================================================
# ReferenceGrant - Allow Ceph Dashboard HTTPRoute to reference OAuth2 Proxy Service
# =============================================================================
# Allows HTTPRoute in rook-ceph namespace to route /oauth2/* traffic
# to OAuth2 Proxy service in oauth2-proxy namespace for login flow.
# =============================================================================

apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-rook-to-oauth2-proxy
  namespace: oauth2-proxy
spec:
  from:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    namespace: rook-ceph
  to:
  - group: ""
    kind: Service
    name: oauth2-proxy
