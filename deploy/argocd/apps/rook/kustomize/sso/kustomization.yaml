---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - keycloak-client-saml.yaml
  - dashboard-saml-config.yaml
  # Note: destination-rule.yaml is deployed separately via ApplicationSet
  # only when serviceMesh.enabled=true and provider=istio
