---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # RBAC for HTTPRoute discovery across namespaces
  - rbac.yaml
  # CronJob to apply OAuth2 forward-auth plugin to protected services
  - oauth2-plugin-job.yaml
