---
# =============================================================================
# Envoy Gateway ApplicationSet (Gateway API Controller)
# =============================================================================
# Déploie Envoy Gateway pour Gateway API (nouvelle génération)
# Implémentation officielle d'EnvoyProxy pour Gateway API
# Docs: https://gateway.envoyproxy.io/
# Repo: https://github.com/envoyproxy/gateway
# =============================================================================

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: envoy-gateway
  namespace: argo-cd
  annotations:
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    - merge:
        mergeKeys:
          - environment
        generators:
          # Config de base (valeurs par défaut)
          - git:
              repoURL: https://github.com/gigi206/k8s
              revision: 'HEAD'
              files:
                - path: deploy/argocd/config/config.yaml

          # Config spécifique à l'application
          - git:
              repoURL: https://github.com/gigi206/k8s
              revision: 'HEAD'
              files:
                - path: deploy/argocd/apps/envoy-gateway/config/*.yaml

  template:
    metadata:
      name: envoy-gateway
      namespace: argo-cd
      labels:
        app: envoy-gateway
        environment: '{{ .environment }}'
      annotations:
    spec:
      project: default

      destination:
        server: https://kubernetes.default.svc
        namespace: envoy-gateway-system

      syncPolicy:
        retry:
          limit: 10
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 10m

  templatePatch: |
    spec:
      # Ignore CRD annotations to avoid "Too long" errors (large last-applied-configuration)
      ignoreDifferences:
        # Gateway API CRDs - already managed by gateway-api-controller
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: httproutes.gateway.networking.k8s.io
          jsonPointers:
            - /metadata/annotations
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: gateways.gateway.networking.k8s.io
          jsonPointers:
            - /metadata/annotations
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: grpcroutes.gateway.networking.k8s.io
          jsonPointers:
            - /metadata/annotations
        # Envoy-specific CRDs
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          name: envoyproxies.gateway.envoyproxy.io
          jsonPointers:
            - /metadata/annotations
      syncPolicy:
        syncOptions:
        {{- range .syncPolicy.syncOptions }}
          - {{ . }}
        {{- end }}
      {{- if .syncPolicy.automated.enabled }}
        automated:
          prune: {{ .syncPolicy.automated.prune }}
          selfHeal: {{ .syncPolicy.automated.selfHeal }}
      {{- end }}

      {{- if .features.monitoring.enabled }}
      # Multi-source: Helm chart + PrometheusRules
      sources:
        {{- if .features.cilium.ingressPolicy.enabled }}
        # Cilium Host Ingress Policy (allow 80/443 to nodes for LoadBalancer)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/resources
          directory:
            include: "cilium-host-ingress-policy.yaml"
        {{- end }}
        {{- if .features.cilium.defaultDenyPodIngress.enabled }}
        # Cilium Pod Ingress Policy (allow traffic to pods)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/resources
          directory:
            include: "cilium-ingress-policy.yaml"
        {{- end }}
        {{- if .features.kyverno.enabled }}
        # Source: Kyverno PolicyException (allows SA token mount for K8s API access)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/resources
          directory:
            include: "kyverno-policy-exception.yaml"
        {{- end }}
        # Source 0: Envoy Gateway Helm Chart (OCI)
        - repoURL: oci://docker.io/envoyproxy/gateway-helm
          chart: gateway-helm
          targetRevision: '{{ .envoyGateway.version }}'
          helm:
            releaseName: envoy-gateway
            valuesObject:
              # Deployment configuration
              deployment:
                replicas: {{ .envoyGateway.replicas | default "1" }}
                envoyGateway:
                  resources:
                    requests:
                      cpu: {{ .envoyGateway.resources.requests.cpu | default "100m" }}
                      memory: {{ .envoyGateway.resources.requests.memory | default "256Mi" }}
                    limits:
                      cpu: {{ .envoyGateway.resources.limits.cpu | default "1000m" }}
                      memory: {{ .envoyGateway.resources.limits.memory | default "1Gi" }}

              # Disable certgen Job TTL to prevent ArgoCD self-sync deadlock.
              # The pre-install/pre-upgrade hook Job gets deleted by Kubernetes
              # TTL controller while ArgoCD is still tracking it, causing the
              # sync operation to stall waiting for a ghost Job.
              certgen:
                job:
                  ttlSecondsAfterFinished: ""
                  {{- if .envoyGateway.certgen }}
                  {{- if .envoyGateway.certgen.resources }}
                  resources:
                    requests:
                      cpu: '{{ .envoyGateway.certgen.resources.requests.cpu }}'
                      memory: '{{ .envoyGateway.certgen.resources.requests.memory }}'
                    limits:
                      cpu: '{{ .envoyGateway.certgen.resources.limits.cpu }}'
                      memory: '{{ .envoyGateway.certgen.resources.limits.memory }}'
                  {{- end }}
                  {{- end }}

              # GatewayClass configuration
              config:
                envoyGateway:
                  gateway:
                    controllerName: gateway.envoyproxy.io/gatewayclass-controller
                  # Enable Backend API for HTTPS backends with TLS version control
                  extensionApis:
                    enableBackend: true

              # Service configuration (LoadBalancer pour MetalLB)
              createNamespace: false  # ArgoCD crée le namespace

        # Source 1: PrometheusRules (monitoring)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/kustomize/monitoring
          kustomize:
            commonLabels:
              release: '{{ .features.monitoring.release }}'
        # Source 2: Gateway and Certificate resources
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/kustomize/gateway
          kustomize:
            patches:
              - target:
                  kind: Gateway
                  name: default-gateway
                patch: |
                  - op: replace
                    path: /spec/gatewayClassName
                    value: '{{ .envoyGateway.gatewayClassName | default "envoy" }}'
                  - op: replace
                    path: /spec/listeners/1/hostname
                    value: '*.{{ .common.domain }}'
                  {{- if and .features.loadBalancer.enabled .features.loadBalancer.staticIPs.gateway }}
                  - op: add
                    path: /spec/infrastructure
                    value:
                      annotations:
                        {{- if eq .features.loadBalancer.provider "metallb" }}
                        metallb.universe.tf/loadBalancerIPs: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- else if eq .features.loadBalancer.provider "cilium" }}
                        lbipam.cilium.io/ips: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- else if eq .features.loadBalancer.provider "loxilb" }}
                        loxilb.io/staticIP: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- end }}
                  {{- end }}
              - target:
                  kind: Certificate
                  name: wildcard-k8s-local
                patch: |
                  - op: replace
                    path: /spec/dnsNames
                    value:
                      - '*.{{ .common.domain }}'
                      - '{{ .common.domain }}'
      {{- else }}
      # Sources: Helm chart + optional Cilium policy
      sources:
        {{- if .features.cilium.ingressPolicy.enabled }}
        # Cilium Host Ingress Policy (allow 80/443 to nodes for LoadBalancer)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/resources
          directory:
            include: "cilium-host-ingress-policy.yaml"
        {{- end }}
        {{- if .features.cilium.defaultDenyPodIngress.enabled }}
        # Cilium Pod Ingress Policy (allow traffic to pods)
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/resources
          directory:
            include: "cilium-ingress-policy.yaml"
        {{- end }}
        - repoURL: oci://docker.io/envoyproxy/gateway-helm
          chart: gateway-helm
          targetRevision: '{{ .envoyGateway.version }}'
          helm:
            releaseName: envoy-gateway
            valuesObject:
              deployment:
                replicas: {{ .envoyGateway.replicas | default "1" }}
                envoyGateway:
                  resources:
                    requests:
                      cpu: {{ .envoyGateway.resources.requests.cpu | default "100m" }}
                      memory: {{ .envoyGateway.resources.requests.memory | default "256Mi" }}
                    limits:
                      cpu: {{ .envoyGateway.resources.limits.cpu | default "1000m" }}
                      memory: {{ .envoyGateway.resources.limits.memory | default "1Gi" }}
              certgen:
                job:
                  ttlSecondsAfterFinished: ""
                  {{- if .envoyGateway.certgen }}
                  {{- if .envoyGateway.certgen.resources }}
                  resources:
                    requests:
                      cpu: '{{ .envoyGateway.certgen.resources.requests.cpu }}'
                      memory: '{{ .envoyGateway.certgen.resources.requests.memory }}'
                    limits:
                      cpu: '{{ .envoyGateway.certgen.resources.limits.cpu }}'
                      memory: '{{ .envoyGateway.certgen.resources.limits.memory }}'
                  {{- end }}
                  {{- end }}
              config:
                envoyGateway:
                  gateway:
                    controllerName: gateway.envoyproxy.io/gatewayclass-controller
                  # Enable Backend API for HTTPS backends with TLS version control
                  extensionApis:
                    enableBackend: true
              createNamespace: false
        # Gateway and Certificate resources
        - repoURL: https://github.com/gigi206/k8s
          targetRevision: '{{ .git.revision }}'
          path: deploy/argocd/apps/envoy-gateway/kustomize/gateway
          kustomize:
            patches:
              - target:
                  kind: Gateway
                  name: default-gateway
                patch: |
                  - op: replace
                    path: /spec/gatewayClassName
                    value: '{{ .envoyGateway.gatewayClassName | default "envoy" }}'
                  - op: replace
                    path: /spec/listeners/1/hostname
                    value: '*.{{ .common.domain }}'
                  {{- if and .features.loadBalancer.enabled .features.loadBalancer.staticIPs.gateway }}
                  - op: add
                    path: /spec/infrastructure
                    value:
                      annotations:
                        {{- if eq .features.loadBalancer.provider "metallb" }}
                        metallb.universe.tf/loadBalancerIPs: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- else if eq .features.loadBalancer.provider "cilium" }}
                        lbipam.cilium.io/ips: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- else if eq .features.loadBalancer.provider "loxilb" }}
                        loxilb.io/staticIP: '{{ .features.loadBalancer.staticIPs.gateway }}'
                        {{- end }}
                  {{- end }}
              - target:
                  kind: Certificate
                  name: wildcard-k8s-local
                patch: |
                  - op: replace
                    path: /spec/dnsNames
                    value:
                      - '*.{{ .common.domain }}'
                      - '{{ .common.domain }}'
      {{- end }}
