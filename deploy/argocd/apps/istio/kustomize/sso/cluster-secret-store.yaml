---
# =============================================================================
# ClusterSecretStore - Istio System OIDC Secrets
# =============================================================================
# Permet de lire les secrets OIDC depuis le namespace istio-system.
# Utilisé par les ExternalSecrets pour synchroniser les credentials OIDC (Kiali).
#
# sync-wave: -1 pour être créé avant les ExternalSecrets qui en dépendent.
# =============================================================================

apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: istio-system-oidc-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  provider:
    kubernetes:
      remoteNamespace: istio-system
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
          namespace: kube-system
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets
