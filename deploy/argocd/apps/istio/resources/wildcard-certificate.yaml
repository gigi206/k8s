---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-gigix
  namespace: istio-system
spec:
  secretName: wildcard-gigix-tls
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
  - "*.gigix"
  - "gigix"
