---
# =============================================================================
# Tempo Configuration - Dev Environment
# =============================================================================
# SingleBinary deployment for development.
# Lightweight, single-pod deployment with local filesystem storage.
# =============================================================================

environment: dev
appName: tempo

tempo:
  namespace: tempo
  version: "1.10.3"  # Helm chart version - Renovate will update this

  # Retention period
  retention: "24h"

  # Resources (actual: ~2m CPU, ~54Mi mem)
  resources:
    requests:
      cpu: "5m"
      memory: "80Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

  # Persistence
  persistence:
    enabled: true
    size: "5Gi"

# Dashboard folder for Grafana
dashboard:
  folder: "Tracing"

# Sync policy for dev (auto-sync enabled)
syncPolicy:
  syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
    - RespectIgnoreDifferences=true
  automated:
    enabled: true
    prune: true
    selfHeal: true
