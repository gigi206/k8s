---
# =============================================================================
# Keycloak Instance - CR Keycloak Operator
# =============================================================================
# Instance Keycloak geree par l'operateur officiel
# Connectee au cluster PostgreSQL CNPG
# =============================================================================

apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  # Nombre d'instances (1 pour dev, 3 pour prod HA)
  instances: 1

  # Configuration de la base de donnees PostgreSQL
  db:
    vendor: postgres
    host: keycloak-db-rw
    port: 5432
    database: keycloak
    usernameSecret:
      name: keycloak-db-credentials
      key: username
    passwordSecret:
      name: keycloak-db-credentials
      key: password

  # Configuration HTTP/HTTPS
  http:
    tlsSecret: keycloak-tls-cert
    httpEnabled: true

  # Hostname configuration - use full HTTPS URL for edge TLS termination
  # See: https://www.keycloak.org/server/hostname
  hostname:
    hostname: https://keycloak.PLACEHOLDER.example.com  # Patched via Kustomize from common.domain
    strict: false
    strictBackchannel: false
    # IMPORTANT: backchannelDynamic must be false so Keycloak uses the configured hostname
    # for all endpoints (token, userinfo, etc.). When true, Keycloak detects the proxy's
    # internal port (e.g., APISIX 9443) and advertises it in OIDC metadata, breaking
    # server-to-server communication from pods that can't reach that port directly.
    backchannelDynamic: false

  # Bootstrap admin credentials (from SOPS secret)
  bootstrapAdmin:
    user:
      secret: keycloak-admin-credentials

  # Disable auto-created Ingress (using HTTPRoute instead)
  ingress:
    enabled: false

  # Proxy configuration (pour Istio/Gateway)
  proxy:
    headers: xforwarded

  # Features additionnelles
  features:
    enabled:
      - docker
      - token-exchange

  # Resources
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "2000m"

---
# =============================================================================
# Certificate pour Keycloak TLS
# =============================================================================

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls-cert
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  secretName: keycloak-tls-cert
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  commonName: keycloak.PLACEHOLDER.example.com  # Patched via Kustomize from common.domain
  dnsNames:
    - keycloak.PLACEHOLDER.example.com  # Patched via Kustomize from common.domain
    - keycloak.keycloak.svc.cluster.local
