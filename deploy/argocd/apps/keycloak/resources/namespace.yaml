---
# =============================================================================
# Namespace with PSA labels for CIS profile compatibility
# =============================================================================
# Deployed conditionally when rke2.cis.enabled=true
# Keycloak operator requires privileged access as the official manifests
# do not include PSA-compliant securityContext settings.
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: keycloak
  labels:
    # PSA: Allow privileged workloads (required for Keycloak operator)
    pod-security.kubernetes.io/enforce: privileged
    # PSA: Warn on restricted violations (visibility into non-compliant pods)
    pod-security.kubernetes.io/warn: restricted
    # PSA: Audit restricted violations (for logging)
    pod-security.kubernetes.io/audit: restricted
