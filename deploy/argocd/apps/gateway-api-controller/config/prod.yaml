---
# Configuration Gateway API Controller - Prod
environment: prod
appName: gateway-api-controller

gatewayApiController:
  version: "v1.4.1"
  # CRD path (experimental pour avoir Gateway API v1)
  crdPath: "config/crd/experimental"

syncPolicy:
  automated:
    enabled: false
    prune: false
    selfHeal: false
