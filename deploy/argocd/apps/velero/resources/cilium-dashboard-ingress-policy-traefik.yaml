---
# =============================================================================
# CiliumNetworkPolicy - Velero UI Ingress from Traefik
# =============================================================================
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: velero-ui-ingress-traefik
  namespace: velero
spec:
  description: "Allow ingress to Velero UI from Traefik"
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: velero-ui

  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: traefik
      toPorts:
        - ports:
            - port: "3000"
              protocol: TCP
