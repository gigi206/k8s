---
# =============================================================================
# CiliumNetworkPolicy - Kasten K10 Dashboard Ingress from Istio Gateway
# =============================================================================
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: kasten-dashboard-ingress-istio
  namespace: kasten-io
spec:
  description: "Allow ingress to Kasten K10 Dashboard from Istio Gateway"
  endpointSelector:
    matchLabels:
      component: gateway

  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: istio-system
      toPorts:
        - ports:
            - port: "8000"
              protocol: TCP
