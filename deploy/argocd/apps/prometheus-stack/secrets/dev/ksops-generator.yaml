---
# =============================================================================
# KSOPS Generator - Grafana Admin Credentials Only
# =============================================================================
# Note: Le secret OIDC est maintenant géré par ExternalSecrets
# depuis keycloak namespace vers monitoring namespace
# =============================================================================
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: prometheus-stack-secrets
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./secret.yaml
