---
# =============================================================================
# Calico NetworkPolicy - Prometheus Stack Ingress from Istio Gateway
# Calico equivalent of: cilium-ingress-policy-istio.yaml
# =============================================================================
# Allows access to Prometheus/Grafana/Alertmanager web UIs from Istio Gateway
# Internal communication is handled by calico-ingress-policy.yaml
# =============================================================================
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: prometheus-stack-ingress-istio
  namespace: monitoring
spec:
  selector: all()
  types:
    - Ingress
  ingress:
    # Allow from Istio Gateway (istio-system namespace)
    - action: Allow
      protocol: TCP
      source:
        namespaceSelector: 'kubernetes.io/metadata.name == "istio-system"'
      destination:
        ports:
          - 3000    # Grafana web UI
          - 9090    # Prometheus web UI / API
          - 9093    # Alertmanager web UI / API
