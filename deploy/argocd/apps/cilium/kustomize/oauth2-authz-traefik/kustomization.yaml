---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - middleware.yaml

# Note: ExtensionRef filter patches for HTTPRoutes are applied via
# ApplicationSet templatePatch using JSON patches on the httproute source.
